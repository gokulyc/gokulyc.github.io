---
import BlogPost from "../../layouts/BlogPostV2.astro";
import { fetchBlogPostData } from "../../utils/api";
import type { BlogPostV2Type } from "../../utils/types";

export const prerender = false;
const { id = "N/A" } = Astro.params;
console.log("Id", id);

const post: BlogPostV2Type = await fetchBlogPostData(id);
console.log(post);
---

<BlogPost {...post} />
